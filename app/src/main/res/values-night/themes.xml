<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Prueba_Beat_on_Jeans" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

        <item name="android:icon">@mipmap/ic_launcher_dark</item>

        <!-- Color primario (barra de acciones, botones, etc.) -->
        <item name="colorPrimary">@color/orange_dark</item>
        <item name="colorPrimaryVariant">@color/orange_dark</item>

        <item name="android:textColor">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="textAppearanceButton">@color/white</item>

        <!-- Color secundario (switch, checkboxes, etc.) -->
        <item name="colorSecondary">@color/orange_dark</item>
        <item name="colorSecondaryVariant">@color/orange_dark</item>

        <item name="android:textStyle"></item>

        <item name="android:textColorHighlightInverse">@color/orange_dark</item>


    </style>
</resources>